import { ConstructSystem } from "../utils/ConstructSystem.js";
export class DebugMessage extends ConstructSystem {
    Init(runtime) {
        super.Init(runtime);
    }
    Update(runtime) {
        //super.Update(runtime);
    }
    Event(runtime) {
        //super.Event(runtime);
    }
    static CreatMessage(runtime) {
    }
    static PutMessage(runtime) {
    }
    static CheckMessageListNumber() {
    }
    static ClearMessageListNumber() {
    }
}
